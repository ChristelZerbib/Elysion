{% extends 'base.html.twig' %}

{% block title %}Edit AlloysList{% endblock %}

{% block body %}
    <h1>Edit AlloysList</h1>

    {{ include('alloys_list/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('alloys_list_index') }}">back to list</a>

    {{ include('alloys_list/_delete_form.html.twig') }}
{% endblock %}
