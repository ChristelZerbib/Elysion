{% extends 'base.html.twig' %}

{% block body %}
    <h1>Modifier le glyphe ou l'amélioration</h1>

    {{ include('glyphs_list/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <br> 
	{{ include('glyphs_list/_delete_form.html.twig') }}
	<br>
    <a class="btn btn-dark text-info border-info btn-block" href="{{ path('glyphs_list_index') }}">Retour à la liste des glyphes et améliorations</a>

    
{% endblock %}
