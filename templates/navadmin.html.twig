<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top"> 
        <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
    			<ul class="navbar-nav mr-auto">
      				<li class="nav-item active">
        				<a class="nav-link text-info" href="{{ path('accueil') }}">Retour <span class="sr-only">(current)</span></a>
     				</li>
				    <li class="nav-item">
        				<a class="nav-link" href="{{ path('admin') }}">Boutiques</a>
      			</li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Personnages et utilisateurs
                </a>
                <div class="dropdown-menu alert-info" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item text-ocean" href="{{ path('user_index') }}">Utilisateurs</a>
                    <a class="dropdown-item text-ocean" href="#">Personnages</a>
                  </div>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Salaires & Loteries</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('pending_validations_index') }}">Validations</a>
            </li>
          </ul>
          <ul class="navbar-nav ml-auto">
             {% if is_granted('ROLE_USER') %}
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle text-info" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Bonjour, {{ app.user.username }}
                </a>
                <div class="dropdown-menu alert-info" aria-labelledby="navbarDropdown">
                  <a class="dropdown-item text-ocean" href="#">Validations</a>
                  <a class="dropdown-item text-ocean" href="{{ path('user_password', {id: app.user.id}) }}">Mot de passe</a>
                  <a class="dropdown-item text-ocean" href="{{ path('app_logout') }}">Déconnexion</a>
                </div>
            </li>
            {% else %}
              <li class="nav-item mr-sm-2">
                <a class="nav-link text-info" href="{{ path('app_login') }}">Connexion</a>
              </li>
            {% endif %}
            
          </ul>
    		</div>
</nav>