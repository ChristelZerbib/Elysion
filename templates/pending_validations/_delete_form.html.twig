<form method="post" action="{{ path('pending_validations_delete', {'id': pending_validation.id}) }}" onsubmit="return confirm('La requête est-elle terminée ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pending_validation.id) }}">
    <button class="btn">Supprimer</button>
</form>
