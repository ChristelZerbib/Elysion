{% extends 'base.html.twig' %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom d'utilisateur</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Permissions</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>PP</th>
                <td>{{ user.pp }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-dark text-info border-info btn-block" href="{{ path('user_index') }}">Retour à la liste des utilisateurs</a>

    <a  class="btn btn-dark text-info border-info btn-block" href="{{ path('user_edit', {'id': user.id}) }}">Modifier l'utilisateur</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
